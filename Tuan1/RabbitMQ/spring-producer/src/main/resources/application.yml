spring:
  application:
    name: spring-producer
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:user}
    password: ${RABBITMQ_PASS:password}

server:
  port: 8080

app:
  queues:
    order: ${ORDER_QUEUE:order_queue}
    dlq: ${ORDER_QUEUE_DLQ:order_queue.dlq}

management:
  endpoints:
    web:
      exposure:
        include: health,info
