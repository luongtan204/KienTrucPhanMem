services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: mydb
    ports:
      - "3310:3306" # Map host 3310 to container 3306
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
